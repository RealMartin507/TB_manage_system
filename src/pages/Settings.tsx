import { Typography } from "antd";

export default function Settings() {
  return (
    <div style={{ padding: 24 }}>
      <Typography.Title level={4}>系统配置</Typography.Title>
      <Typography.Text type="secondary">Phase 后续实现</Typography.Text>
    </div>
  );
}
